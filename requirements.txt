# Core
anthropic>=0.40.0
httpx>=0.27.0

# Tracing (Arize AX)
openinference-instrumentation-anthropic>=0.1.0
arize-otel>=0.7.0
opentelemetry-sdk>=1.20.0

# LangGraph (optional, for full workflow)
langgraph>=0.2.0
langchain-core>=0.3.0

# Development
pytest>=8.0.0
pytest-asyncio>=0.24.0
black>=24.0.0
ruff>=0.6.0
mypy>=1.11.0

# Optional: for embedding-based thread detection
# sentence-transformers>=3.0.0
# scikit-learn>=1.5.0
