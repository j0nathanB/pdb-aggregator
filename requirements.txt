# Core
anthropic>=0.40.0
httpx>=0.27.0
browserbase>=1.7.0
playwright>=1.40.0
trafilatura>=2.0.0  # HTML → article text extraction (used by curl/playwright/browserbase extractors)
json-repair>=0.25.0  # auto-repair LLM JSON output (missing commas, trailing commas, etc.)
jinja2>=3.1.0  # prompt templating for structured editors
pydantic>=2.0.0  # structured models / validation
pyyaml>=6.0  # YAML config loading (country configs, extraction routing, settings)
python-dotenv>=1.0.0  # .env loader

# Error monitoring
sentry-sdk>=2.0.0

# Tracing (Arize AX)
openinference-instrumentation-anthropic>=0.1.0
arize-otel>=0.7.0
opentelemetry-sdk>=1.20.0

# LangGraph (optional, for full workflow)
langgraph>=0.2.0
langchain-core>=0.3.0

# Development
pytest>=8.0.0
pytest-asyncio>=0.24.0
black>=24.0.0
ruff>=0.6.0
mypy>=1.11.0

# NLP / validation
spacy>=3.8.0,<4.0.0

# Event clustering
sentence-transformers>=2.2.0
hdbscan>=0.8.33
numpy>=1.24.0
scipy>=1.10.0
python-dateutil>=2.8.0
