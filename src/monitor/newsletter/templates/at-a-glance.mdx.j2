---
title: "The Middle Powers at a Glance"
description: "Week of {{ date_range }}"
sidebarTitle: "At a Glance"
---

<Note>
Top story per country this week, ranked by coverage volume. Select a story to visit the full country summary.
</Note>

{% for region in regions %}
{% if region.countries %}

## [{{ region.display_name }}](/briefs/{{ end_date_str }}/{{ region.slug }})

{% for c in region.countries %}
<a href="/briefs/{{ end_date_str }}/{{ region.slug }}#{{ c.name | lower | replace(' ', '-') }}" style={{ "{{ textDecoration: 'none', color: 'inherit' }}" }}>
<img src="https://flagcdn.com/{{ c.code }}.svg" alt="{{ c.name }} flag" style={% raw %}{{ width: "20px", height: "15px", display: "inline", verticalAlign: "middle", marginRight: "6px" }}{% endraw %} /> **{{ c.name }}** — {{ c.headline }}
</a>

{% endfor %}
{% endif %}
{% endfor %}
